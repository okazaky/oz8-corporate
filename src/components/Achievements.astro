---
import { getCurrentLocale, getTranslations } from '../i18n/translations';

const currentPath = Astro.url.pathname;
const locale = getCurrentLocale(currentPath);
const t = getTranslations(locale);

const achievements = [
  {
    title: t.achievements.makuake,
    description: t.achievements.makuakeDesc,
    icon: '🚀'
  },
  {
    title: t.achievements.anan,
    description: t.achievements.ananDesc,
    icon: '📰'
  },
  {
    title: t.achievements.amazon,
    description: t.achievements.amazonDesc,
    icon: '📦'
  },
  {
    title: t.achievements.survey,
    description: t.achievements.surveyDesc,
    icon: '📊'
  }
];
---

<section class="py-16 md:py-20 bg-white">
  <div class="max-w-6xl mx-auto px-6 md:px-8">
    <h2 class="text-3xl md:text-4xl font-bold text-center text-[#333333] mb-12">
      {t.achievements.title}
    </h2>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
      {achievements.map((achievement) => (
        <div class="bg-white p-8 border border-gray-200 rounded-lg">
          <div class="flex items-start">
            <div class="text-4xl mr-4 flex-shrink-0">{achievement.icon}</div>
            <div class="flex-1">
              <h3 class="text-xl font-semibold text-[#333333] mb-2">
                {achievement.title}
              </h3>
              <p class="text-[#666666] leading-relaxed">
                {achievement.description}
              </p>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
