---
import { getCurrentLocale, getTranslations } from '../i18n/translations';

const currentPath = Astro.url.pathname;
const locale = getCurrentLocale(currentPath);
const t = getTranslations(locale);

const badges = [
  {
    title: t.trust.established,
    description: t.trust.establishedDesc,
    icon: '📅'
  },
  {
    title: t.trust.guarantee,
    description: t.trust.guaranteeDesc,
    icon: '✓'
  },
  {
    title: t.trust.supervised,
    description: t.trust.supervisedDesc,
    icon: '👨‍⚕️'
  },
  {
    title: t.trust.rating,
    description: t.trust.ratingDesc,
    icon: '⭐'
  }
];
---

<section class="py-20 bg-gray-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <h2 class="text-4xl md:text-5xl font-extrabold text-center text-[#503AA8] mb-16">
      {t.trust.title}
    </h2>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
      {badges.map((badge, index) => (
        <div class="group relative bg-white p-8 rounded-2xl shadow-lg hover:shadow-2xl transform hover:-translate-y-2 transition-all duration-300">
          <!-- Yellow accent line -->
          <div class="absolute top-0 left-0 w-full h-2 bg-gradient-to-r from-[#FFEE58] to-[#FFE91F] rounded-t-2xl"></div>

          <div class="text-center">
            <div class="text-5xl mb-6 group-hover:scale-110 transition-transform">{badge.icon}</div>
            <h3 class="text-xl font-bold text-[#503AA8] mb-3">
              {badge.title}
            </h3>
            <p class="text-sm text-gray-600 leading-relaxed">
              {badge.description}
            </p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
