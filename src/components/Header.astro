---
import { getCurrentLocale, getTranslations, getAlternateUrl } from '../i18n/translations';

const currentPath = Astro.url.pathname;
const locale = getCurrentLocale(currentPath);
const t = getTranslations(locale);

const alternateLocale = locale === 'ja' ? 'en' : 'ja';
const alternateUrl = getAlternateUrl(currentPath, alternateLocale);

const navItems = [
  { href: locale === 'en' ? '/en/' : '/', label: t.nav.home },
  { href: locale === 'en' ? '/en/about' : '/about', label: t.nav.about },
  { href: locale === 'en' ? '/en/achievements' : '/achievements', label: t.nav.achievements },
  { href: locale === 'en' ? '/en/contact' : '/contact', label: t.nav.contact }
];
---

<header class="sticky top-0 bg-white border-b border-gray-200 z-50">
  <nav class="max-w-6xl mx-auto px-6 md:px-8">
    <div class="flex items-center justify-between h-16">
      <!-- Logo -->
      <div class="flex-shrink-0">
        <a href={locale === 'en' ? '/en/' : '/'} class="text-xl font-bold text-[#333333]">
          OZ8 {locale === 'ja' ? '株式会社' : 'Corporation'}
        </a>
      </div>

      <!-- Desktop Navigation -->
      <div class="hidden md:flex items-center space-x-8">
        {navItems.map((item) => (
          <a
            href={item.href}
            class="text-[#666666] hover:text-[#8B7355] text-sm font-medium"
          >
            {item.label}
          </a>
        ))}

        <!-- Language Switch -->
        <a
          href={alternateUrl}
          class="text-[#666666] hover:text-[#8B7355] text-sm font-medium"
        >
          {locale === 'ja' ? 'EN' : 'JA'}
        </a>
      </div>

      <!-- Mobile Menu Button -->
      <button
        type="button"
        class="md:hidden p-2"
        aria-label="Toggle menu"
        id="mobile-menu-button"
      >
        <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>
    </div>

    <!-- Mobile Menu -->
    <div class="hidden md:hidden pb-4" id="mobile-menu">
      <div class="flex flex-col space-y-2">
        {navItems.map((item) => (
          <a
            href={item.href}
            class="text-[#666666] hover:text-[#8B7355] text-sm font-medium px-4 py-2"
          >
            {item.label}
          </a>
        ))}
        <a
          href={alternateUrl}
          class="text-[#666666] hover:text-[#8B7355] text-sm font-medium px-4 py-2 border-t border-gray-200 mt-2 pt-4"
        >
          {locale === 'ja' ? 'English' : '日本語'}
        </a>
      </div>
    </div>
  </nav>
</header>

<script>
  // Mobile menu toggle
  document.addEventListener('DOMContentLoaded', () => {
    const button = document.getElementById('mobile-menu-button');
    const menu = document.getElementById('mobile-menu');

    button?.addEventListener('click', () => {
      menu?.classList.toggle('hidden');
    });
  });
</script>
